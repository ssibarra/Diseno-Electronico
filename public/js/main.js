//Enlazar el codigo lo que se hara es que cuando este listo va a ejecutar una funcion la cual sera todo el prox code
/*$(document).ready(function Tabs(dtlinicio,dtlfinal){
	$('ul.tabs li a:first').addClass('active');
	$('.secciones article').hide(); /* Ocultamos a todos */
	/*$('.secciones article:first').show();

	$('ul.tabs li a').click(function(){          /* Cuando le undamos click a un enlace se va a ejecutar una funcion */
		/*$('ul.tabs li a').removeClass('active');
		$(this).addClass('active');
		$('.secciones article').hide();          /* Cuando hagamos clik todas las secciones se ocultan */
		/*var activeTab = $(this).attr('href');  /* Cuando le undamos click va a traer el atributo del href que sea */
		/*TheTab(activeTab);*/
		/*console.log(activeTab);
		$(activeTab).show();					 /* Se muestra el tab seleccionado */
		
		/*if ((dtlinicio == '') || (dtlfinal == '') || (dtlinicio == dtlfinal) || (dtlfinal < dtlinicio)) {
			alert('Dear user, please type the form correctly');
			} else {
			// Fragmentación de datos
			var initialdate = dtlinicio[0].concat(dtlinicio[1],dtlinicio[2],dtlinicio[3],dtlinicio[5],dtlinicio[6],dtlinicio[8],dtlinicio[9],dtlinicio[11],dtlinicio[12],dtlinicio[14],dtlinicio[15]); 
			console.log(initialdate);
			var finaldate = dtlfinal[0].concat(dtlfinal[1],dtlfinal[2],dtlfinal[3],dtlfinal[5],dtlfinal[6],dtlfinal[8],dtlfinal[9],dtlfinal[11],dtlfinal[12],dtlfinal[14],dtlfinal[15]); 
			console.log(finaldate);

			}

	});  
}); */